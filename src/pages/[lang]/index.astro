---
import { locales } from '../../i18n/ui';
export function getStaticPaths(){ return locales.map((lang) => ({ params:{lang} })); }
const lang = Astro.params.lang;
return Astro.redirect(`/${lang}/uses/`);
---
