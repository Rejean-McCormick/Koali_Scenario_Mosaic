---
import metadata from '../../data/scenarios.json';
export function getStaticPaths(){ return Object.keys(metadata).map((id) => ({ params:{id} })); }
return Astro.redirect(`/en/uses/${Astro.params.id}/`);
---
