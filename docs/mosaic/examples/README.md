# Mosaic example fragments

These files mirror the current public-profile implementation for documentation purposes.

**The executable repository root is authoritative.** If an example here ever differs from `src/`, follow the root implementation.

The v3.2 public hover profile intentionally uses human-readable scenario indicators—activity, scale, context, and special conditions—rather than internal Koali component gauges.
