# ADR-003 — SVG over Canvas/WebGL

**Decision:** use inline SVG for the mosaic.

**Reasons:** only 120 elements, interactive DOM, native links, focus, ARIA, straightforward CSS.

**Rejected alternative:** Canvas/WebGL, which are disproportionate to the need.
