# ADR-004 — No UI framework for the MVP

**Decision:** use native TypeScript inside Astro.

**Reasons:** simple interaction model, small bundle, easy maintenance.

**Alternative:** React/Vue/Svelte can be added locally later if a future feature genuinely requires one.
