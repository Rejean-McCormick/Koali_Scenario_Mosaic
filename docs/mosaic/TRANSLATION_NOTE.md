# English Edition Note

This directory is the English edition of `KOALI_USE_MOSAIC_UI_DOCS_v1`.

The translation preserves:

- document structure and filenames;
- technical decisions;
- IDs and routes;
- architecture and implementation contracts;
- Astro/TypeScript/CSS/SVG examples;
- MVP boundaries.

The wording is intentionally natural English rather than line-by-line literal translation.

Where the example UI contained user-facing French copy, it has been localized to English. Technical identifiers such as `SCN-001`, component names, route structure, and field names remain stable.
