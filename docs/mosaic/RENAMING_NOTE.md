# Naming policy

This subsystem is named **Koali Use Mosaic**.

A separate naming term is reserved elsewhere in the wider Koali ecosystem and must not be reused here.

Use `Mosaic` in public copy and `mosaic-*` in technical identifiers. The public route remains `/uses/`.
